import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    marginBottom: 20,
  },
  picker: {
    height: 40,
    borderWidth: 1,
    borderColor: '#aaa',
  },
});
