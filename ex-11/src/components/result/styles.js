import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  result: {
    fontSize: 18,
    textAlign: 'center',
    fontWeight: 'bold',
    color: 'green',
    marginTop: 10,
  },
});
