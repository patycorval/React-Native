import { StyleSheet } from 'react-native'

const styles = new StyleSheet.create({
  input:{
    width: "100%",
    borderWidth: 1,
    borderColor: '#222',
    padding: 10,
    borderRadius: 10
  }
})

export default styles